# hexview

A couple of functions for displaying numerical data (preferably byte arrays)
as a hex dump (a la xxd).

## Usage

To hexdump all values in a sequence my-seq to *out*:

(hexview my-seq)

or, to hexdump the first, say, 10 lines:

(def hexdump-lines (hexview-lines my-seq))
(println (apply str (take 10 hexdump-lines)))

## License

Distributed under the Eclipse Public License, the same as Clojure.
